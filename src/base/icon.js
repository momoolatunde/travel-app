import React from 'react';
import AntDesign from 'react-native-vector-icons/AntDesign';

export default function AntDesignIcon({icon, size, color}) {
  return <AntDesign name={icon} size={size} color={color} />;
}
